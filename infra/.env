# ============================================================
# LOCAL DEVELOPMENT — Docker Postgres containers
# ============================================================

# ---------- Database Host ----------
# For local: use docker container names as hostnames
DB_HOST=will-be-overridden
# NOTE: In local mode, each service uses its own container name
# (user-db, product-db, cart-db) via depends_on in docker-compose.local.yml
# The DB_HOST in the base compose is overridden per-service because
# each service talks to a DIFFERENT Postgres container.
# We handle this by setting DB_HOST per container below.

# ---------- User DB ----------
USER_DB_NAME=shoppers_user
USER_DB_USER=postgres
USER_DB_PASSWORD=postgres

# ---------- Cart DB ----------
CART_DB_NAME=shoppers_cart
CART_DB_USER=postgres
CART_DB_PASSWORD=postgres

# ---------- Product DB ----------
PRODUCT_DB_NAME=shoppers_product
PRODUCT_DB_USER=postgres
PRODUCT_DB_PASSWORD=postgres

# ---------- Spring JPA ----------
SPRING_JPA_HIBERNATE_DDL_AUTO=validate

# ---------- Erlang Broker ----------
ERLANG_BROKER_PORT=5555
SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION= "true"
